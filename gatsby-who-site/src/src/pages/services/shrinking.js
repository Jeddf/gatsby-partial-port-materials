import React from "react"
import Layout from "../../components/Layout"

export default () => (
  <Layout breadcrumbs={["Services", "Shrinking"]}>
    <h1>Shrinking Service</h1>
    <h2>Some trees are just too big. Let's sort it out.</h2>
  </Layout>
)
